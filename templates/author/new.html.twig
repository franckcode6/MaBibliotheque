{% extends 'base.html.twig' %}

{% block title %}Nouvel auteur{% endblock %}

{% block body %}
    {{ form_start(formAuthor) }}
    {{ form_rest(formAuthor) }}
    <button type="submit" class="btn btn-success">Valider</button>
    {{ form_end(formAuthor) }}
{% endblock %}